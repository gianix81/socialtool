# TikTok adapter — future work

Placeholder for the future `TikTokSocialProvider` implementing
`SocialProvider` (see `../social-provider.ts`) against the TikTok Content
Posting API. No implementation, endpoints or credentials are defined yet.

Must be registered in `../provider-registry.ts` for `TIKTOK` once ready.
