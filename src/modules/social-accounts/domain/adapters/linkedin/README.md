# LinkedIn adapter — future work

Placeholder for the future `LinkedInSocialProvider` implementing
`SocialProvider` (see `../social-provider.ts`) against the LinkedIn Marketing
API. No implementation, endpoints or credentials are defined yet.

Must be registered in `../provider-registry.ts` for `LINKEDIN` once ready.
