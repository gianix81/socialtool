import { getWorkspace } from "@/modules/workspaces/current";import { Heading,WorkspacePicker } from "@/modules/shared/ui";
export default async function Analytics({searchParams}:{searchParams:{workspace?:string}}){const {memberships,current}=await getWorkspace(searchParams.workspace);return <><Heading title="Analytics" subtitle="Metriche dimostrative coerenti con il provider mock."/><WorkspacePicker memberships={memberships} currentId={current?.id} path="/analytics"/><div className="grid gap-4 sm:grid-cols-3">{[["Impression","12.480","+8,2%"],["Interazioni","934","+4,1%"],["Tasso engagement","7,5%","+0,6%"]].map(([a,b,c])=><div className="card" key={a}><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold">{b}</p><p className="mt-1 text-sm text-emerald-600">{c}</p></div>)}</div></>}

